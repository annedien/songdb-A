$("ul.errors").html("")
<% if @song.errors.any? %>
<% @song.errors.full_messages.each do |message| %>
$("ul.errors").append($("<li />").html("<%= message.html_safe %>"))
<% end %>
<% else %>
$(".song-show").html("<%= escape_javascript(render 'index') %>")
$("#song-modal").modal("hide")
<% end %>