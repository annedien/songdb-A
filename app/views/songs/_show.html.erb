<% @artist.songs.each do |song| %>
    <tr>
      <td><%= song.name %></td>
      <td><%= link_to "Delete", "songs/#{song.id}/delete", remote: true, class: "btn btn-danger" %></td>
    </tr>
<% end %>